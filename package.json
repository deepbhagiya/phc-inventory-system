{
  "name": "phc-inventory-system",
  "version": "1.0.0",
  "description": "Professional PHC Inventory System",
  "scripts": {
    "setup": "npm install && cd server && npm install && npm run seed && cd ../client && npm install",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
